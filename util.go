package main

/*
func PrintAllPoynts() {
	for k, _ := range poynts {
		PrintPoyntsForKey(k)
	}
}


func PrintPoyntsForKey(k string) {
	if p, ok := poynts[k]; ok {
		for i, v := range p {
			fmt.Println("Index:", i, "Name:", v.Name, "Logt:", v.Logt, "Obst:", v.Obst, "Opt:", v.Opt)
		}
	}
}
*/
